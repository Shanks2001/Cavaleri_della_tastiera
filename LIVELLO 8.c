#include<stdio.h>
#include <string.h>

#define max 100
int main(){
    char scrivi[max];

    printf("scrivi cosa manca\n");
   //i++,%d,;
    printf("int vet[10];\nfor(int i=0;i<n;){\nprintf(\"scrivi i valori :\", i + 1);\nscanf(\"%%d\", &vet[i]);\nint primo = 1\nfor(j=vet[i]-1; j>1; j--){\nif(vet[i]%%j==0){\nprimo = 0;\n}\");\n");
    printf("1:");
    fgets(scrivi, sizeof(scrivi), stdin);
    printf("2:");
    fgets(scrivi, sizeof(scrivi), stdin);
    printf("3:");
    fgets(scrivi, sizeof(scrivi), stdin);

    memset(scrivi, '\0', sizeof(scrivi));

    printf("scrivi cosa manca \n");//=,",",;
    printf("if(primo = 1){\nprintf(\"numero primo\" j);\n} else {\nprintf(\"numero non primo\", j)\n}\n");
    printf("1:");
    fgets(scrivi, sizeof(scrivi), stdin);
    printf("2:");
    fgets(scrivi, sizeof(scrivi), stdin);
    printf("3:");
    fgets(scrivi, sizeof(scrivi), stdin);

    memset(scrivi, '\0', sizeof(scrivi));

    printf("scrivi cosa manca\n");//for,i++,",",i
    printf("(int i = 0; i < 10; ){\nprintf(\"scrivi un valore %%d:\" i + 1);\nscanf(\"%%d\", &sequenza[]);\n}\n");
    printf("1:");
    fgets(scrivi, sizeof(scrivi), stdin);
    printf("2:");
    fgets(scrivi, sizeof(scrivi), stdin);
    printf("3:");
    fgets(scrivi, sizeof(scrivi), stdin);
    printf("4:");
    fgets(scrivi, sizeof(scrivi), stdin);
    printf("LIVELO 9");

    return 0;
}